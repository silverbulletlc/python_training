# 实例084：连接字符串
# 题目：连接字符串。
# 程序分析：无。
delimiter = ','
mylist = ['Brazil', 'Russia', 'India', 'China']
print(delimiter.join(mylist))

# 知识点：join()方法——'sep'.join(sep_object)
# 参数说明
# sep：分割符，可为“，、；”等。
# sep_object：分割对象，可为字符串、以及储存字符串的元组、列表、字典。